# Repository ADRs

This folder contains architecture decisions that govern the repository or multiple features.

Do not put feature-only decisions here. Feature-specific ADRs belong with the feature under `in-progress/<feature>/adr/` or `features/<feature>/adr/`.

Use a numbered filename such as:

```text
001-feature-development-model.md
002-published-library-taxonomy.md
```

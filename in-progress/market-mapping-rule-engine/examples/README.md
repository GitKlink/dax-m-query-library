# Market Mapping Rule Engine Examples

Examples in this feature must use synthetic identifiers/data only.

Recommended starting examples:

- exact title + job-profile replacement;
- title suffix + job-profile replacement;
- state + job-profile replacement;
- ALL-scope manager suffix conversion;
- AddColumn reconciliation usage;
- Microsoft List normalisation pattern (source adapter, not engine logic).

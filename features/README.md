# Completed Features

This folder contains the permanent engineering record for completed features.

A completed feature preserves its README, approved spec, ADRs, tests, examples, and other useful design artifacts. The canonical reusable implementation itself remains under `library/`.

Completed feature folders should retain the same slug used while under `in-progress/`.

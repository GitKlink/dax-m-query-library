# Published Library

This folder contains canonical reusable assets that are ready to consume.

```text
m/       Power Query (M)
dax/     DAX
python/  Python
deneb/   Deneb / Vega / Vega-Lite
```

Do not place active design notes, experiments, or competing WIP implementations here. Those belong with the feature under `in-progress/`.

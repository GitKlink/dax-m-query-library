# Power Query (M)

Published Power Query assets.

```text
functions/  Directly reusable custom functions
patterns/   Reusable transformation/design patterns that require adaptation
```

Group functions by natural M namespace (for example `table/`, `list/`, `record/`) when enough assets exist to justify the folder.
